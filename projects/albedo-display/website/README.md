# Public website

Implementation lives in `src/pages/AlbedoDisplay.tsx` and `src/pages/AlbedoDisplayInvest.tsx`, published at `/#/display` and `/#/display/invest` under the site's current HashRouter. Public pages must never expose `data-room/`. Replace concept imagery with dated real evidence as it becomes available.
